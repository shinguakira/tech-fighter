# オンライン対戦のシグナリングサーバー URL。
#
# ・サーバー一体運用（推奨・Render/Fly/Railway に `npm start` を1個デプロイ）:
#     未設定でOK。フロントと同じオリジンの Socket.IO に自動接続する。
#
# ・フロントを別ホスト（Vercel 等）に置く場合:
#     ここに常駐サーバーの URL を設定する（Vite のビルド時に埋め込まれる）。
# VITE_SIGNALING_URL=https://your-tech-fighter.onrender.com
